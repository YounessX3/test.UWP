<Page
    x:Class="test.UWP.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!-- Style for ToggleButtons (similar to your ButtonRevealStyle but for ToggleButton) -->
        <!-- Dark theme background -->
        <RevealBackgroundBrush x:Key="DarkRevealBackgroundBrush" Color="Black" TargetTheme="Dark" />

        <!-- Light theme background -->
        <RevealBackgroundBrush x:Key="LightRevealBackgroundBrush" Color="White" TargetTheme="Light" />
        <Style x:Key="ToggleButtonRevealStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                                <FontIcon FontFamily="Segoe MDL2 Assets"
                                          FontSize="36"
                                          Glyph="{TemplateBinding Tag}"
                                          Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock Text="{TemplateBinding Content}" FontSize="18" FontWeight="SemiBold"/>
                                    <TextBlock Text="{TemplateBinding ToolTipService.ToolTip}" FontSize="12" Opacity="0.7"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SystemBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DevicesBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NetworkInternetBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PersonalizationBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AppsBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccountsBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TimeBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GamingBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="UpdateBtnStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="120"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="{ThemeResource ButtonRevealBackground}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ButtonRevealBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}"/>
            <Setter Property="UseSystemFocusVisuals" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid x:Name="RootGrid" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPointerOver}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPointerOver}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonRevealBackgroundPressed}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonRevealBorderBrushPressed}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Opacity" Value="0.4"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <VisualState.Setters>
                                            <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                            <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <StackPanel Margin="10,0" Orientation="Horizontal" VerticalAlignment="Center">
                                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="36" Glyph="{TemplateBinding Tag}" Margin="0,0,12,0"/>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" FontSize="18" Text="{TemplateBinding Content}"/>
                                    <TextBlock FontSize="12" Opacity="0.7" Text="{TemplateBinding ToolTipService.ToolTip}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Your existing SystemToggleRevealStyle can remain or be reused for BtnSystem -->
    </Page.Resources>



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Search Box Row -->
            <RowDefinition Height="Auto"/>
            <!-- Home Label Row -->
            <RowDefinition Height="*"/>
            <!-- Buttons Grid Row -->
        </Grid.RowDefinitions>

        <!-- Search box centered at the top -->
        <Border Background="{ThemeResource HomeSearchBackgroundBrush}" Padding="0,10" Grid.Row="0">
            <AutoSuggestBox x:ConnectionId='111'
        x:Name="SuggestBox"
        PlaceholderText="Search..."
        QueryIcon="Find"
        HorizontalAlignment="Center"
        Margin="0,10"
        Width="300"/>
        </Border>

        <Border Background="{ThemeResource HomeSearchBackgroundBrush}" Padding="36,10,0,10" Grid.Row="1">
            <TextBlock
        Text="Home"
        FontSize="36"
        FontWeight="SemiBold"
        VerticalAlignment="Center"/>
        </Border>



        <!-- Buttons grid -->
        <Grid
    HorizontalAlignment="Center"
    VerticalAlignment="Center"
    Width="720"
    Height="380"
    Grid.Row="2">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- System Button -->
            <ToggleButton x:ConnectionId='102' x:Name="BtnSystem"
          Content="System"
          Tag="&#xE770;"
          ToolTipService.ToolTip="Display, sound, notifications"
          Style="{StaticResource SystemBtnStyle}"
          Grid.Row="0" Grid.Column="0" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- changed here -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Devices Button -->
            <ToggleButton x:ConnectionId='103' x:Name="BtnDevices"
          Content="Devices"
          Tag="&#xE8B8;"
          ToolTipService.ToolTip="Bluetooth, printers, mouse"
          Style="{StaticResource DevicesBtnStyle}"
          Grid.Row="0" Grid.Column="1" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Network & Internet Button -->
            <ToggleButton x:ConnectionId='104' x:Name="BtnNetwork"
          Content="Network &amp; Internet"
          Tag="&#xE701;"
          ToolTipService.ToolTip="Wi-Fi, airplane mode, VPN"
          Style="{StaticResource NetworkInternetBtnStyle}"
          Grid.Row="0" Grid.Column="2" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Personalization Button -->
            <ToggleButton x:ConnectionId='105' x:Name="BtnPersonalization"
          Content="Personalization"
          Tag="&#xE771;"
          ToolTipService.ToolTip="Background, lock screen, colors"
          Style="{StaticResource PersonalizationBtnStyle}"
          Grid.Row="1" Grid.Column="0" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Apps Button -->
            <ToggleButton x:ConnectionId='106' x:Name="BtnApps"
          Content="Apps"
          Tag="&#xE71D;"
          ToolTipService.ToolTip="Uninstall, defaults, features"
          Style="{StaticResource AppsBtnStyle}"
          Grid.Row="1" Grid.Column="1" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Accounts Button -->
            <ToggleButton x:ConnectionId='107' x:Name="BtnAccounts"
          Content="Accounts"
          Tag="&#xE77B;"
          ToolTipService.ToolTip="Email, sync, work or school"
          Style="{StaticResource AccountsBtnStyle}"
          Grid.Row="1" Grid.Column="2" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Time & Language Button -->
            <ToggleButton x:ConnectionId='108' x:Name="BtnTime"
          Content="Time &amp; Language"
          Tag="&#xE823;"
          ToolTipService.ToolTip="Speech, region, date"
          Style="{StaticResource TimeBtnStyle}"
          Grid.Row="2" Grid.Column="0" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Gaming Button -->
            <ToggleButton x:ConnectionId='109' x:Name="BtnGaming"
          Content="Gaming"
          Tag="&#xE7FC;"
          ToolTipService.ToolTip="Game bar, captures, streaming"
          Style="{StaticResource GamingBtnStyle}"
          Grid.Row="2" Grid.Column="1" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

            <!-- Update & Security Button -->
            <ToggleButton x:ConnectionId='110' x:Name="BtnUpdate"
          Content="Update &amp; Security"
          Tag="&#xE895;"
          ToolTipService.ToolTip="Windows Update, recovery"
          Style="{StaticResource UpdateBtnStyle}"
          Grid.Row="2" Grid.Column="2" Margin="10,1" BorderThickness="2,2,2,2"
                               >
                <!-- added -->
                <ToggleButton.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.ThemeDictionaries>
                            <ResourceDictionary x:Key="Light">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FFFFFFFF" TargetTheme="Light"/>
                            </ResourceDictionary>
                            <ResourceDictionary x:Key="Dark">
                                <RevealBackgroundBrush x:Key="ButtonRevealBackgroundPointerOver" Color="#FF000000" TargetTheme="Dark"/>
                            </ResourceDictionary>
                        </ResourceDictionary.ThemeDictionaries>
                    </ResourceDictionary>
                </ToggleButton.Resources>
            </ToggleButton>

        </Grid>

    </Grid>
</Page>

