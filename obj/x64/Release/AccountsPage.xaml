<Page
    x:Class="test.UWP.AccountsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:test.UWP"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Accounts" FontSize="36" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- Input Row -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Spacing="12">
            <TextBox x:ConnectionId='4' x:Name="AccountInput" Width="300" PlaceholderText="Enter account name..." />
            <Button x:ConnectionId='5' Content="Add Account"                         />
        </StackPanel>

        <!-- List of accounts -->
        <ListView x:ConnectionId='2' x:Name="AccountsListView" Grid.Row="2" Margin="0,20,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="18" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

